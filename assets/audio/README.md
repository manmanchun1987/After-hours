# Audio assets（內容音效）

自合成免版權細檔，對應現有 Web Audio 行為：

| 檔 | 用途 |
|---|---|
| `bgm-loop.mp3` | 深夜低音量 loop BGM |
| `sfx-click.mp3` | UI click |
| `sfx-choice.mp3` | 選擇 |
| `sfx-transition.mp3` | 轉場／節點切換 |

接線由「邏輯上線」改 `app.js`；本目錄唔改主邏輯。見 `manifest.json`。
